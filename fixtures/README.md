# Fixtures

`silan-dev-fixtures.json` is generated from `lib/silan-viking/fixtures.ts` with:

```bash
npm run fixtures:export
```

The JSON snapshot exists for Desktop and projection integration checks. The
typed TypeScript fixtures remain the source used by the application and tests.
